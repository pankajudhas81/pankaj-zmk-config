# FilePath: .gitignore

.DS_Store
practice.txt
.claude/
.venv/

# ZMK local build workspace (created by `just build` / west)
/zmk/
/zephyr/
/modules/
/tools/
/bootloader/
/.west/
/build/
/optional/

# Locally-built firmware (timestamped dirs)
/firmware/
